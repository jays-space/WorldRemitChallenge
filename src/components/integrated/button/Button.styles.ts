import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    paddingHorizontal: 18,
    paddingVertical: 12,
    borderColor: '#C1C1C1',
    borderWidth: 1,
    borderRadius: 6,
  },
});
